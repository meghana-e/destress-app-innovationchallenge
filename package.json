{
  "name": "destress-app",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "backend": "cd backend && python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 4000 --env-file .env",
    "dev": "npm run build:frontend && npm run backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}